<!-- **
自分では、星つけれない。
** -->
<% unless current_user == scomment.user %>

    <!-- **
    もし、ログイン中のユーザーがすでに星をつけていたら、星の結果が表示される。
    ** -->
    <% if current_user.rating?(scomment) %>
        <%= render 'ratings/show', scomment: scomment %>

    <!-- **
    まだ星をつけていなければ、空の星が表示される。
    ** -->
    <% else %>
        <%= render 'ratings/create', scomment: scomment %>
    <% end %>

<% end %>
