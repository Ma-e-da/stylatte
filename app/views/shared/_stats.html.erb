<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong class="stat">
      <%= @user.following.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong class="stat">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
  <a href="<%= current_user.posts.count %>">
    <strong class="stat">
      <%= @current_user.posts.count %>
    </strong>
    posts
  </a>
  <a href="#">
    <strong class="stat">
      <%= @user.followers.count %>
    </strong>
    keeps
  </a>
  <a href="#">
    <strong class="stat">
      <%= @user.followers.count %>
    </strong>
    stylings
  </a>
  <a href="#">
    <strong class="stat">
      <%= @user.followers.count %>
    </strong>
    stars
  </a>
</div>
