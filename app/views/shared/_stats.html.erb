<div class="ui segment">
Statページスタート＞＞
<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong class="stat">
      <%= @user.following.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong class="stat">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
  <a href="<%= posts_path(@user) %>">
    <strong class="stat">
      <%= @user.posts.count %>
    </strong>
    posts
  </a>
  <div class="ui labeled button" tabindex="0">
    <div class="ui button">
      <i class="heart icon"></i><%= link_to "My favorites", favorites_path(current_user) %>
    </div>
    <a class="ui basic label">
      <%= @user.favorites.count %>
    </a>
  </div>

ここは、このままでは、starをつけた人のレコードを数えている。(starをもらった人のレコード数にする。)
      <i class="star icon"></i> Star
      <%= @user.ratings.count %>



</div>
＜＜Statページおわり
</div>
