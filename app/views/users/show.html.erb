＞＞users/showページはじまり。
だれからもユーザーを探すをしたら見れるページ。

<!-- ユーザーの自己紹介 -->
  <%= gravatar_for @user %>
  <%= @user.name %>
<!-- フォロー、アンフォローボタン -->
  <%= render 'follow_form' if logged_in? %>

<!-- 星の合計 -->
  <h2>今までもらった星の数!</h2>
  <i class="star icon"><%= @total_rating %></i>


<!-- ユーザーがスタイリストの場合、スタイリングブックをみせる。 -->
  <% if @user.stylist? %>
  <h2>This is My StylingBook!</h2>
  #
  <% end %>

<!-- ポストを公開している場合、一覧を見せる。 -->
    <ol class="posts">
      <%= render @posts %>
    </ol>
