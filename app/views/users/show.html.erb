＞＞users/showページはじまり。(だれからもユーザーを探すをしたら見れるページ。)

<div class="ui two column stackable grid">

<!-- ユーザー情報 -->
<div class="four wide column">
  <!-- ユーザーの自己紹介 -->
  <div class="ui card">
    <div class="userimg">
      <!-- ラベル、スタイリストならステータスと共に変わる。 -->
      <% if logged_in? && @user.stylist? %>
        <a class="ui red left corner label">
          <i class="heart icon"></i>
        </a>
      <% end %>
      <div class="image">
        <%= link_to image_tag("stylatte_50.png", alt: "STYLATTE"), root_path %>
      </div>
      <span>
        <%= gravatar_for @user %>
      </span>
    </div>

    <div class="content">
      <div class="header">
        <%= @user.name %>
      </div>
      <div class="description">
        #Matthew is an interior designer living in New York.#
      </div>
    </div>
    <div class="extra content">
    <!-- フォロー、アンフォローボタン -->
    <%= render 'follow_form' if logged_in? %>
    </div>

  </div>

  <!-- ユーザーがスタイリストの場合、スタイリングブックをみせる。 -->
  <% if @user.stylist? %>
  <h2>This is My StylingBook!</h2>
  #
  <% end %>


</div>

<!-- ポストを公開している場合、一覧を見せる。 -->
  <div class="twelve wide column">
    <ol class="posts">
      <%= render @posts %>
    </ol>
  </div>

</div>
